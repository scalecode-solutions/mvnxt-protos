// DO NOT EDIT.
// swift-format-ignore-file
// swiftlint:disable all
//
// Generated by the Swift generator plugin for the protocol buffer compiler.
// Source: mvservernxt/v1/audience.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/apple/swift-protobuf/

/// Audience is the server-internal audience descriptor stored alongside
/// every event-log entry whose delivery is bounded to a specific set of
/// users. The ws-broadcast subscriber reads it to resolve recipients;
/// future subscribers (push, audit, search) read the same descriptor.
///
/// This is the proto shape for doc 07's capability-token concept. Slice-1
/// ships the unsigned form with Kind=LIST (explicit user set). Later
/// slices add:
///   - Signing (HMAC over the canonical bytes, stored in `signature`)
///   - Dynamic kinds (GROUP resolved via conversation members, CONTACTS
///     via contact graph, etc.) once those domains exist
///
/// Envelope column on the events row is bytea; we proto-encode an Audience
/// into it. No JSON intermediate, no string keys — typed end to end.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
import SwiftProtobuf

// If the compiler emits an error on this type, it is because this file
// was generated by a version of the `protoc` Swift plug-in that is
// incompatible with the version of SwiftProtobuf to which you are linking.
// Please ensure that you are building against the same version of the API
// that was used to generate this file.
fileprivate struct _GeneratedWithProtocGenSwiftVersion: SwiftProtobuf.ProtobufAPIVersionCheck {
  struct _2: SwiftProtobuf.ProtobufAPIVersion_2 {}
  typealias Version = _2
}

/// AudienceKind enumerates how recipients are derived. More kinds land
/// without renumbering.
public enum Mvservernxt_V1_AudienceKind: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// PUBLIC: every user that can see the containing aggregate. Used for
  /// things like public pulse posts. No include/exclude — the aggregate
  /// itself defines visibility.
  case `public` // = 1

  /// LIST: explicit user set. `include` holds the uuids; `exclude` may
  /// remove some. Slice-1 chat uses this — the resolver just reads
  /// `include`. Simple, verifiable, no external lookups.
  case list // = 2

  /// CONTACTS: caller's contact graph. `context["owner_id"]` names the
  /// owner whose contacts expand here. Resolved when subscriber fans
  /// out; not slice-1.
  case contacts // = 3

  /// GROUP: current active members of a conversation. Context carries
  /// `conv_id`. Resolved by the subscriber querying chat store. Not
  /// slice-1 — chat currently enumerates members into LIST at append
  /// time so fan-out is stable even if membership changes later.
  case group // = 4

  /// USER: exactly one recipient. Equivalent to LIST with include=[one].
  case user // = 5
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .public
    case 2: self = .list
    case 3: self = .contacts
    case 4: self = .group
    case 5: self = .user
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .public: return 1
    case .list: return 2
    case .contacts: return 3
    case .group: return 4
    case .user: return 5
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Mvservernxt_V1_AudienceKind] = [
    .unspecified,
    .public,
    .list,
    .contacts,
    .group,
    .user,
  ]

}

/// Audience is the typed descriptor. Store proto-encoded bytes in the
/// `events.audience` bytea column.
public struct Mvservernxt_V1_Audience: Sendable {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// Schema version. Bump when a breaking change ships.
  public var version: Int32 = 0

  public var kind: Mvservernxt_V1_AudienceKind = .unspecified

  /// Explicit recipients (LIST, USER).
  /// UUID strings — matches the rest of our wire where IDs are strings.
  public var include: [String] = []

  /// Removals applied after kind-specific resolution.
  public var exclude: [String] = []

  /// Kind-specific context (GROUP's conv_id, CONTACTS' owner_id, etc.).
  public var context: Dictionary<String,String> = [:]

  /// HMAC over the canonical bytes of (version, kind, include, exclude,
  /// context). Empty until signing lands. Future verifiers MUST fail
  /// closed on missing signature once the server's key material is
  /// wired up.
  public var signature: Data = Data()

  public var unknownFields = SwiftProtobuf.UnknownStorage()

  public init() {}
}

// MARK: - Code below here is support for the SwiftProtobuf runtime.

fileprivate let _protobuf_package = "mvservernxt.v1"

extension Mvservernxt_V1_AudienceKind: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0AUDIENCE_KIND_UNSPECIFIED\0\u{1}AUDIENCE_KIND_PUBLIC\0\u{1}AUDIENCE_KIND_LIST\0\u{1}AUDIENCE_KIND_CONTACTS\0\u{1}AUDIENCE_KIND_GROUP\0\u{1}AUDIENCE_KIND_USER\0")
}

extension Mvservernxt_V1_Audience: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  public static let protoMessageName: String = _protobuf_package + ".Audience"
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{1}version\0\u{1}kind\0\u{1}include\0\u{1}exclude\0\u{1}context\0\u{1}signature\0")

  public mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    while let fieldNumber = try decoder.nextFieldNumber() {
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every case branch when no optimizations are
      // enabled. https://github.com/apple/swift-protobuf/issues/1034
      switch fieldNumber {
      case 1: try { try decoder.decodeSingularInt32Field(value: &self.version) }()
      case 2: try { try decoder.decodeSingularEnumField(value: &self.kind) }()
      case 3: try { try decoder.decodeRepeatedStringField(value: &self.include) }()
      case 4: try { try decoder.decodeRepeatedStringField(value: &self.exclude) }()
      case 5: try { try decoder.decodeMapField(fieldType: SwiftProtobuf._ProtobufMap<SwiftProtobuf.ProtobufString,SwiftProtobuf.ProtobufString>.self, value: &self.context) }()
      case 6: try { try decoder.decodeSingularBytesField(value: &self.signature) }()
      default: break
      }
    }
  }

  public func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    if self.version != 0 {
      try visitor.visitSingularInt32Field(value: self.version, fieldNumber: 1)
    }
    if self.kind != .unspecified {
      try visitor.visitSingularEnumField(value: self.kind, fieldNumber: 2)
    }
    if !self.include.isEmpty {
      try visitor.visitRepeatedStringField(value: self.include, fieldNumber: 3)
    }
    if !self.exclude.isEmpty {
      try visitor.visitRepeatedStringField(value: self.exclude, fieldNumber: 4)
    }
    if !self.context.isEmpty {
      try visitor.visitMapField(fieldType: SwiftProtobuf._ProtobufMap<SwiftProtobuf.ProtobufString,SwiftProtobuf.ProtobufString>.self, value: self.context, fieldNumber: 5)
    }
    if !self.signature.isEmpty {
      try visitor.visitSingularBytesField(value: self.signature, fieldNumber: 6)
    }
    try unknownFields.traverse(visitor: &visitor)
  }

  public static func ==(lhs: Mvservernxt_V1_Audience, rhs: Mvservernxt_V1_Audience) -> Bool {
    if lhs.version != rhs.version {return false}
    if lhs.kind != rhs.kind {return false}
    if lhs.include != rhs.include {return false}
    if lhs.exclude != rhs.exclude {return false}
    if lhs.context != rhs.context {return false}
    if lhs.signature != rhs.signature {return false}
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}
